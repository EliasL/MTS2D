# Project instructions

- Do not use `std::numeric_limits`: it is incompatible with this project's
  optimization level. Use the C floating-point limits from `<cfloat>` (for
  example, `DBL_MAX`) instead.
